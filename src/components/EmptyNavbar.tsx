// EmptyNavbar.tsx - A completely empty navbar component
// This component exists only to replace the original Navbar without errors

const EmptyNavbar = () => {
  return null;  // Return nothing - completely empty component
};

export default EmptyNavbar;
